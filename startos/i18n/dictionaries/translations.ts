import { LangDict } from './default'

// English-only for the staging package; add languages here if this ever
// graduates beyond internal testing.
export default {} satisfies Record<string, LangDict>
